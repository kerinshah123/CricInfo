<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.criinfo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".More.teamPlayers"></activity>
        <activity android:name=".More.addPlayer" />
        <activity android:name=".More.createTeam" />
        <activity android:name=".More.teamManagerTeam" />
        <activity android:name=".Home.HomeActivity" />
        <activity
            android:name=".Home.MatchDetails"
            android:label="MatchDetails"
            android:parentActivityName=".Home.HomeActivity" />
        <activity
            android:name=".LoginSignUpActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>